
export const textFilterHandle = (value) => {
    if (value === "") {
      setResources(data);
    } else {
      // setResources(
      //   data.filter((item) => {
      //     return item.owner.toUpperCase().includes(value.toUpperCase());
      //   })
      // );
    }
  };

export const selectFilterHandle = (data, filter) =>{
    
}